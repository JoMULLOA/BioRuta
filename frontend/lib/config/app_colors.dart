import 'package:flutter/material.dart';

class AppColors {
  // ==================== MODO CLARO ====================
  static const Color primaryLight = Color(0xFF5c63cc);       // Morado-azul principal
  static const Color lightBackground = Color(0xFFF8F9FF);    // Fondo claro
  static const Color lightSurface = Color(0xFFFFFFFF);       // Superficie clara
  static const Color lightText = Color(0xFF1A1A1A);          // Texto claro
  static const Color lightSecondaryText = Color(0xFF6B7280); // Texto secundario claro
  static const Color lightProfileBox = Color(0xFF5c63cc);    // Caja de perfil modo claro
  
  // ==================== MODO OSCURO ====================
  static const Color primaryDark = Color(0xFF10124A);        // Azul oscuro elegante para AppBar
  static const Color darkBackground = Color(0xFF0B0D23);     // Azul muy oscuro de fondo
  static const Color darkSurface = Color(0xFF1A1C35);        // Azul oscuro suave para tarjetas
  static const Color darkText = Color(0xFFFFFFFF);           // Texto blanco puro
  static const Color darkSecondaryText = Color(0xFFBDBDBD);  // Texto secundario gris claro
  static const Color darkProfileBox = Color(0xFF0B0D3A);     // Azul oscuro principal para caja de perfil
  static const Color darkAccent = Color(0xFF10124A);         // Azul oscuro de acento
  
  // ==================== COLORES UNIVERSALES ====================
  static const Color white = Color(0xFFFFFFFF);              // Blanco puro
  static const Color success = Color(0xFF4CAF50);            // Verde éxito
  static const Color warning = Color(0xFFFF9800);            // Naranja advertencia
  static const Color error = Color(0xFFD32F2F);              // Rojo error
}